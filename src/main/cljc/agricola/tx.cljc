(ns agricola.tx
  (:require
   [datahike.api :as d]))

(defn insert-optional [& {:keys [title tx]}])

(defn add-grain [db])

(defn remove-grain [db entity-id n-grain] db)

(defn remove-grain [db entity-id n-grain] db)
