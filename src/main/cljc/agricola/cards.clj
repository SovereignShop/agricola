(ns agricola.cards)

(def grain-elevator [:agricola.card/name :card-names/grain-elevator])
(def field-watchman [:agricola.card/name :card-names/field-watchman])
(def family-counseler [:agricola.card/name :card-names/family-counseler])
